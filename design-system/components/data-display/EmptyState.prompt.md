Reusable empty pattern for no results, no bookings and offline lists.

```jsx
<EmptyState icon="ticket" title="No bookings yet" body="When you reserve a tour it will show up here."
  action={<Button variant="primary">Browse tours</Button>} />
```
