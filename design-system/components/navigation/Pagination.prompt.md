Paged catalogue navigation — chosen over infinite scroll so users on metered data control what loads.

```jsx
<Pagination page={2} pages={5} resultsLabel="Showing 13–24 of 48 tours" onChange={setPage} />
```
