Sticky top bar. Desktop shows the nav inline; mobile collapses to hamburger plus badge and pairs with BottomNav.

```jsx
<Header items={[{label:"Tours",href:"/tours"},{label:"Regions",href:"/regions"}]} current="/tours" signedIn />
```
