Persistent price breakdown, visible at every checkout step; it always ends with the exact charge sentence.

```jsx
<OrderSummary lines={[{label:"GH₵450 × 4 travellers",amount:1800}]} total={1800} approxTotal={115} payMode="later" sticky />
```
