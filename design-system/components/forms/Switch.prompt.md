Immediate on/off preference (notifications, data-saver). Never use it inside a form that has a Save button — use a Checkbox there.

```jsx
<Switch id="lite" label="Data saver" description="Load smaller photos on slow connections" checked />
```
