Top-of-form error list for a failed step submit; takes focus so screen-reader users hear the failure immediately.

```jsx
<ErrorSummary errors={[{id:"email",message:"Enter an email address"}]} />
```
