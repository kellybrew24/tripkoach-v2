Multi-line input for special requests and dietary notes; vertical resize only.

```jsx
<Textarea rows={4} maxLength={300} placeholder="Anything we should know? Dietary needs, mobility, celebrations…" />
```
